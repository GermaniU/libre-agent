# secretos — NUNCA commitear
.env
*.key
*.pem

# entorno / caché
.venv/
__pycache__/
*.pyc
*.pyo

# datos locales (conversaciones, memoria, trazas, páginas generadas)
cowork.db
*.db
trace.jsonl
*.jsonl
static/*.html

# personal del usuario — NUNCA commitear (usa los .example / _fallback de base)
prompts/soul.md

# sistema
.DS_Store
mcp.json
.pytest_cache/
